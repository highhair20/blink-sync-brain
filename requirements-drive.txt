# Drive Pi - USB Gadget Mode Requirements
# Minimal dependencies for storage and USB gadget functionality

# Core dependencies (minimal)
numpy==1.24.3
Pillow==10.0.1

# Configuration and logging
pyyaml==6.0.1
python-dotenv==1.0.0
structlog==23.1.0

# USB gadget mode support
pyusb==1.2.1

# Network and communication (minimal)
requests==2.31.0

# System monitoring
psutil==5.9.5

# Samba support (if using file sharing)
# No additional packages needed - uses system samba
